{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "message": "chore(release): publish",
      "allowBranch": ["main", "legacy-mainnet-sdk"],
      "ignoreChanges": [
        "*.md"
      ]
    }
  }
}
