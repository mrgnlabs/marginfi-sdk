{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "npmClient": "yarn",
  "command": {
    "publish": {
      "message": "chore(release): Publish",
      "allowBranch": "main",
      "ignoreChanges": [
        "*.md",
        ".*"
      ]
    }
  }
}