{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "changed": "lerna changed",
    "bump": "lerna version --conventional-graduate",
    "publish": "lerna version from-git"
  },
  "devDependencies": {
    "lerna": "^5.0.0"
  }
}